[project]
name = "esp-cam-detector"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "python-multipart>=0.0.9",
    "ultralytics>=8.4.0",
]

[tool.uv]
package = false
